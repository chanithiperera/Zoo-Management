/**
 * Offline-safe placeholder for store products/categories without a remote image.
 * Avoids via.placeholder.com (often blocked → ERR_CONNECTION_CLOSED on LAN / strict networks).
 */
export const STORE_PRODUCT_PLACEHOLDER = require('../../assets/module-images/module-store.png');
