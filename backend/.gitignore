# Dependencies
node_modules/

# Environment (never commit)
.env
.env.*
!.env.example
!.env.sample

# Logs
*.log
npm-debug.log*

# Uploaded files (keep directory with .gitkeep)
src/uploads/*
!src/uploads/.gitkeep
